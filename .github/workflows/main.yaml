name: My Action

on:
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run script
        run: |
          npm install
          npm install -g gatsby-cli
          gatsby build
          echo "Build complete"
          ls

